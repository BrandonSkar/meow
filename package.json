{
  "name": "meow",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx esbuild src/panel.jsx --bundle --outfile=dist/panel.js",
    "start": "npx esbuild src/panel.jsx --bundle --outfile=dist/panel.js --servedir=dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.21.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
